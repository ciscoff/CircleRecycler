<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="s.yzrlykov.circlerecycler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".stages.s03_1.Activity03Layouts"></activity>
        <activity android:name=".stages.s01.Activity01CirclePoints" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".stages.s02.Activity02CirclePoints" />
        <activity android:name=".stages.s03.Activity03CirclePoints" />
        <activity android:name=".stages.s04.Activity04Scrolling" />
    </application>

</manifest>